Implement:
+1, -1, null

+1, -1 => int

? +, -

1) Комментарии убирать где и когда?
    Игнорировать коментарии (можно регекспом попробовать убрать)

    В каком виде комментарии? - Зуеву

2) Что делаем с переходами на новую строку? 
    Можно просто убрать при считывании файла (Пока сделал так)

3) Реализовать определение типов

4) quote - Как будем реализовывать? ' - отдельный токен или целиком весь синтаксический элемент? Пример: '(plus 1 2)

5) Про Span: 
    Зависит от того какой функционал вы хотите включить. Спан - это просто координаты токена в исходном файле. Вы можете его использовать, например, чтобы указывать в какой строке и на каком символе возникла ошибка компиляции

Убрать удаление переходов на новую строку, добавить в спан номер строки.

6) Как мы будем определять list?

7) Делаем ли отдельный класс под каждый токен?

8) Literal : [+|-] Integer | [+|-] Real | Boolean | null -- На каком моменте это распознается?


9) Момент с quote / ': 
    Все что '(...) / 'x и т.д. берем как один токен
    А как поступаем с quote?